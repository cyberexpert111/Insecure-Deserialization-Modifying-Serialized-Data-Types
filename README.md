# Insecure-Deserialization-Modifying-Serialized-Data-Types
The application trusted serialized data received from the client without performing sufficient integrity or type validation, creating an opportunity for attackers to manipulate values that should never be client-controlled.
